# Copy these to Render Dashboard → Environment
TELEGRAM_BOT_TOKEN=1234567890:ABCDEF...your_token...
# Negative number for supergroups (e.g., -1001234567890). You can get it using @RawDataBot or logging update.effective_chat.id
TELEGRAM_GROUP_ID=-1001234567890

# Where to send files (a folder within your union remote). Change if you edit rclone.conf template.
RCLONE_DEST=free_union:/telegram

# WebUI authentication
RCLONE_USER=admin
RCLONE_PASS=supersecret

# Paste the entire content of your rclone.conf here (include [sections], tokens, etc.)
RCLONE_CONFIG_CONTENT=
